## 📝 Deployment and Use

1. Clone the repository

```yaml
git clone https://github.com/abokecn/serverslist.git
```

2. Customization
- **Add Servers**: Edit `servers.js` to include your own server data.
- **Update Providers**: Modify `providers.js` to add/remove cloud services.

3. Deploy to your website


## 📝部署使用

1. 克隆仓库

```yaml
git clone https://github.com/abokecn/serverslist.git
```

2. 自定义设置
- **添加服务器**：编辑 `servers.js` 文件以添加您自己的服务器数据。
- **更新云服务提供商**：修改 `providers.js` 文件以添加/移除云服务。

3. 部署到您的网站


## 📝デプロイと使用方法

1. リポジトリのクローン作成

```yaml
git clone https://github.com/abokecn/serverslist.git
```

2. 設定のカスタマイズ
- **サーバーの追加**: `servers.js` ファイルを編集して、独自のサーバーデータを追加します。
- **クラウドサービスプロバイダーの更新**: `providers.js` ファイルを編集して、クラウドサービスを追加/削除します。

3. ウェブサイトへのデプロイ
